/**
 * START ADS ENGINE - Hub Multiplataforma JS
 */

document.addEventListener('DOMContentLoaded', () => {
  // Inicializa ícones Lucide
  if (window.lucide) {
    window.lucide.createIcons();
  }

  console.log('[Start Ads Engine] Hub Multiplataforma carregado.');
});
